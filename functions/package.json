{
  "name": "mindkraft-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Mindkraft reminder delivery",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "luxon": "^3.5.0",
    "web-push": "^3.6.7"
  }
}
